# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: ['*']
  push:
    branches: ['*']

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    name: Build and Test
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.11.11, 2.12.8, 2.12.9, 2.12.10, 2.12.11, 2.13.0, 2.13.1, 2.13.2]
        java: [adopt@1.8, adopt@1.14]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (generic)
        uses: actions/cache@v1
        with:
          path: ~/.coursier/cache/v1
          key: ${{ runner.os }}-generic-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (linux)
        if: contains(runner.os, 'linux')
        uses: actions/cache@v1
        with:
          path: ~/.cache/coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (macOS)
        if: contains(runner.os, 'macos')
        uses: actions/cache@v1
        with:
          path: ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (windows)
        if: contains(runner.os, 'windows')
        uses: actions/cache@v1
        with:
          path: ~/AppData/Local/Coursier/Cache/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Check that workflows are up to date
        run: sbt ++${{ matrix.scala }} githubWorkflowCheck

      - run: sbt ++${{ matrix.scala }} test docs/mdoc

      - name: Upload target directory 'target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-target
          path: target

      - name: Upload target directory 'generated-docs/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-generated-docs_target
          path: generated-docs/target

      - name: Upload target directory 'project/target'
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target
          path: project/target

  publish:
    name: Publish Artifacts
    needs: [build]
    if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/master')
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.2]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (generic)
        uses: actions/cache@v1
        with:
          path: ~/.coursier/cache/v1
          key: ${{ runner.os }}-generic-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (linux)
        if: contains(runner.os, 'linux')
        uses: actions/cache@v1
        with:
          path: ~/.cache/coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (macOS)
        if: contains(runner.os, 'macos')
        uses: actions/cache@v1
        with:
          path: ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache coursier (windows)
        if: contains(runner.os, 'windows')
        uses: actions/cache@v1
        with:
          path: ~/AppData/Local/Coursier/Cache/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Download target directory 'target' (2.11.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.11.11-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'target' (2.12.8)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.8-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'target' (2.12.9)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.9-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'target' (2.13.0)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.0-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-target
          path: target

      - name: Download target directory 'generated-docs/target' (2.11.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.11.11-${{ matrix.java }}-generated-docs_target
          path: generated-docs/target

      - name: Download target directory 'generated-docs/target' (2.12.8)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.8-${{ matrix.java }}-generated-docs_target
          path: generated-docs/target

      - name: Download target directory 'generated-docs/target' (2.12.9)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.9-${{ matrix.java }}-generated-docs_target
          path: generated-docs/target

      - name: Download target directory 'generated-docs/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-generated-docs_target
          path: generated-docs/target

      - name: Download target directory 'generated-docs/target' (2.12.11)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.11-${{ matrix.java }}-generated-docs_target
          path: generated-docs/target

      - name: Download target directory 'generated-docs/target' (2.13.0)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.0-${{ matrix.java }}-generated-docs_target
          path: generated-docs/target

      - name: Download target directory 'generated-docs/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-generated-docs_target
          path: generated-docs/target

      - name: Download target directory 'generated-docs/target' (2.13.2)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.2-${{ matrix.java }}-generated-docs_target
          path: generated-docs/target

      - name: Download target directory 'project/target'
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target
          path: project/target

      - name: Publish project
        run: sbt ++${{ matrix.scala }} +publish